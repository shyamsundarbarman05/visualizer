# Copy this file to `.env` and adjust the value to match your deployed backend.
# When deploying the backend and frontend separately, set this to the backend API root.
REACT_APP_API_BASE_URL=https://your-production-domain/api
